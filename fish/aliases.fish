# Vim
balias vim "nvim"

# rm
balias rm "rmtrash"

# ls
balias sl "ls"

# For Android
balias dumpsys="adb shell dumpsys"
balias activityrecord="dumpsys activity or grep -B 1 \"Run #[0-9]*:\""
balias fragmentrecord="dumpsys activity top or grep -B 1 \"#[0-9]*:\""

# adb
balias adb "adb-peco"
